#transpose of a matrix
import numpy as np
def Transpose_matrix(d):
 t=np.zeros((d.shape[0],d.shape[0]))
 for i in range(d.shape[0]):
  for j in range(d.shape[0]):
    t[j][i]=d[i][j]   
 return d   
 
#Transpose_matrix(np.arange(1,26).reshape(5,5))
